import './Button.css';

const Button = (): JSX.Element => {
    return (
        <button
            className='button accent'
        >
            Сохранить
        </button>
    );
};

export default Button;